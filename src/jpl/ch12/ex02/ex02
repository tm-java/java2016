・PassengerVehicleオブジェクトの定員を負の値に設定しようとした
	falseの戻り値
	(理由)予想できる上、何かの処理でバグを修正できると思うから

・オブジェクトがその初期値を設定する設定ファイルに、文法エラーが見つかった
	Exception
	(理由)予想はできるが、プログラマからは回避不可能なので、例外を投げる

・プログラマが指定した単語を文字列の配列から検索するメソッドが、その単語を発見できない
	falseの戻り値
	(理由)検索失敗は予想でき、かつエラーでもないと思うから
	//2017.3.3　
	//そもそも発見できた時何を返すの？インデックスなら、発見できなかった時は-1返すとか

・openメソッドへ指定されたファイルが存在しない
	FileNotFoundException
	(理由)予想はできるが、プログラマでは回避不可能であるから
	
・openメソッドへ指定されたファイルは存在するが、セキュリティにより使用できない
	IOException
	(理由)予想しにくい、かつプログラマでは回避不可能であるから

・リモートのサーバプロセスにネットワークコネクションを確立しようとするが、リモートのマシンと接続できない
	falseの戻り値
	(理由)予想はできるため、通信が失敗した旨をプログラマに知らせる
	//2017.3.3
	//予測はできるけど気をつけて欲しいからチェックされる例外を投げたら？
	
・リモートのサーバプロセスとのやり取りの最中に、ネットワークコネクションが切れる
	Exception
	(理由)予想はできるが、プログラマでは回避不可能であるから
	//2017.3.3
	//予想はできるけど、気をつけて欲しいから、チェックされる例外
	